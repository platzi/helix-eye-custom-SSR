import styled from 'styled-components';

export const StyledGalaxiesLayout = styled.div`
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 320px));
  row-gap: 2rem;
  column-gap: 5rem;
  justify-content: center;
  width: 100%;
`